# マニュアル (日本語版)

*[English version here](MANUAL.md)*

## 基本の使い方

キーボードで数式を入力し、**Enter**キーで評価します。

```
2*sin(pi/4)+sqrt(16)
```

結果が下に `= 5.414213562` のように表示されます。過去の計算履歴は入力欄の上に薄い色で表示され、画面から見えなくなった分も含めて直近20件がメモリに保持されます。

## 基本の編集操作

| キー | 動作 |
|---|---|
| 文字・数字・演算子 | カーソル位置に挿入 |
| `Backspace` | カーソルの直前の1文字を削除 |
| `fn` + `Backspace` | 数式・結果を全て消去 |
| `fn` + `,` | カーソルを左に移動 |
| `fn` + `/` | カーソルを右に移動 |
| `Tab` | カーソル直前の関数名を補完(下記参照) |
| `Enter` | 評価(計算実行) |

入力中に表示される `|` はカーソル位置です。末尾からしか編集できないわけではなく、途中の任意の位置を編集できます。

## 結果が表示された後の挙動

`= ...` が表示された状態で次に押すキーによって動作が変わります。

- **演算子**(`+ - * / ^ %`)を押すと、直前の結果から続けて計算します(通常の電卓の連続計算と同じ)。例: `+` → `3` → `Enter` で直前の答えに3を足す
- **それ以外のキー**(数字・文字・`(`など)を押すと、直前の式は破棄されて新しい入力になります
- **`Backspace`** を押すと、直前の式を**消去せずに編集モードへ戻します**。タイプミスの修正や数値の微調整をしてから再計算したいときに便利です。もう一度Backspaceを押すと通常通り末尾の1文字が削除されます

## 履歴機能

- `fn` + `;` — 1つ古い履歴を呼び出して編集欄に読み込みます(連打でさらに遡れます。`;`キーには上向き矢印が印刷されています)
- `fn` + `.` — 新しい方向へ戻ります。最新を超えると空の入力欄に戻ります(`.`キーには下向き矢印が印刷されています)
- 呼び出した履歴はそのまま編集可能です。内容を変更してEnterを押せば再計算できます
- `ans` / `ans(n)`(下記の関数一覧を参照)を使うと、履歴全体を呼び出さなくても、過去の**結果の値だけ**を新しい式の中で使えます

## Tabキーによる関数名補完

関数名を入力中に**Tab**キーを押すと補完されます。

```
si<Tab>   ->  sin(
at<Tab>   ->  atan(      (もう一度Tab: atan2( 、さらに: atanh( 、さらに: atan( に戻る)
```

引数を取らない語(`pi`、`e`、`ans`、`help`)は、括弧を付けずに補完されます。他のキーを押すと補完の候補送りは終了します。

## DEG / RAD(度数法・弧度法)の切り替え

`opt` + `D` で、すべての三角関数(`sin cos tan asin acos atan atan2`)の角度の単位を度数法・弧度法で切り替えます。現在のモードは画面左上に `[DEG]` または `[RAD]` と表示されます。

## 本体上でのヘルプ表示

`help` と入力してEnterを押すと、画面上に関数一覧のリファレンスが表示されます。

- `fn` + `;` / `fn` + `.` — ページ送り
- `Enter` または `Backspace` — 電卓画面に戻る

## 履歴の保存方法

**自動保存(内蔵フラッシュ)**: 計算を行うたびに、履歴(直近20件)とDEG/RAD設定がESP32内蔵のフラッシュメモリ(NVS)に自動保存され、起動時に自動的に復元されます。SDカードは不要で、電源を完全に切っても履歴は消えません。

**手動での書き出し(microSDカード)**: `save` と入力してEnterを押します。microSDカードが挿さっていれば、現在の履歴が `/calc_log.txt` にテキストとして追記されます。

```
---- save #1 (3 entries, RAD) ----
2+3 = 5
10*2 = 20
ans(1)+ans(2) = 25

```

`save` を実行するたびに新しいブロックが**追記**されるので(上書きではありません)、繰り返し実行すればログが積み重なっていきます。ただし、その時点で画面に表示されている履歴をそのまま書き出す仕組みなので、間に新しい計算をせずに`save`を2回実行すると、同じ内容が重複して記録される点は注意してください。ファイルはパソコンにmicroSDカードリーダーで挿せばテキストとして読めます。SDカードが無い、または初期化に失敗した場合は、結果の代わりに `SD ERR` と表示されますが、それ以外の動作には影響しません。

## 時刻の設定

この基板にはRTC(時計)チップが搭載されておらず、このスケッチにもWi-Fi/NTPの仕組みは入っていないため、設定しない限り時計は存在しません。

- `settime(H,M,S)` — 現在時刻をセットします(24時間制。例: 朝9時30分0秒なら `settime(9,30,0)`)。この瞬間の電卓の内部経過時間(起動からの時間)に、指定した時刻を紐付けます。
- `time` — セットした時刻に、そこからの経過時間を足した「現在時刻」を表示します。

電源が切れるたびに時計は「未設定」状態にリセットされます。電池でバックアップされた時計ではないため、時刻表示を使いたい場合は**起動のたびに`settime`をやり直してください**。時刻をセットした後は、`save`のログにも記録時刻が付くようになります(例: `---- save #2 (4 entries, RAD) @ 09:47:12 ----`)。

## カードを抜かずにSDカードの中身を見る

`usbdrive` と入力してEnterを押すと、同じUSB-Cケーブル経由でmicroSDカードをそのままパソコンに公開します。普通のUSBドライブとして認識されるので、カードを一度も抜かずに `calc_log.txt` を取り出したり、逆にファイルをカードに書き込んだりできます。

ただしこの機能は、SDカードとUSB接続をその用途に丸ごと明け渡す形になります。**実行すると電卓はキー入力を受け付けなくなり**、元に戻すにはリセットまたは電源の入れ直ししかありません。中間状態は無いので、計算作業が一段落してから使ってください。SDカードが無い、または初期化に失敗した場合は `SD ERR` と表示され、通常動作は継続します。

`usbdrive` 自体は動くのに、パソコン側でカードのマウントに失敗する場合は、電卓をリセットしてから `usbdebug` と入力すると、その時に記録された生のSDエラー内容を確認できます(特定のカードで問題が起きた場合の切り分けに使えます)。

## 自動スリープ

Cardputer / Cardputer ADVには本格的な電源管理チップが無いため、完全な電源オフはできず、代わりに**ディープスリープ**(消費電流が数十マイクロアンペア程度まで下がる、電源オフに最も近い状態)を使います。デフォルトでは、**10分間**キー入力が無いとスリープします。

- `sleeptime(n)` — アイドルタイムアウトを `n` 分に設定(0で自動スリープを無効化)。フラッシュに保存されるので、電源を切っても設定は保持されます。
- `sleeptime`(括弧無し) — 現在の設定値を表示。

**復帰方法**: 本体**側面の物理ボタン「G0/BtnA」を押してください**(キーボードのキーではありません)。スリープ中はキーボードのマトリクス全体への電源供給も止まるため、通常のキー操作では復帰できません。スリープに入る直前に、通常通り履歴と設定はフラッシュへ保存されます。

## 関数リファレンス

三角関数は上記のDEG/RAD設定に従います。

### 三角関数

| 関数 | 説明 | 例 | 結果 |
|---|---|---|---|
| `sin(x)` | 正弦 | `sin(pi/2)` | `1` |
| `cos(x)` | 余弦 | `cos(0)` | `1` |
| `tan(x)` | 正接 | `tan(pi/4)` | `1` |
| `asin(x)` | 逆正弦 | `asin(1)` | `1.570796327` |
| `acos(x)` | 逆余弦 | `acos(0)` | `1.570796327` |
| `atan(x)` | 逆正接 | `atan(1)` | `0.7853981634` |
| `atan2(y,x)` | 座標(x,y)から角度を求める | `atan2(1,1)` | `0.7853981634` |

### 双曲線関数

| 関数 | 説明 | 例 | 結果 |
|---|---|---|---|
| `sinh(x)` | 双曲線正弦 | `sinh(1)` | `1.175201194` |
| `cosh(x)` | 双曲線余弦 | `cosh(0)` | `1` |
| `tanh(x)` | 双曲線正接 | `tanh(1)` | `0.7615941560` |
| `asinh(x)` | 逆双曲線正弦 | `asinh(1)` | `0.8813735870` |
| `acosh(x)` | 逆双曲線余弦(x≧1) | `acosh(1)` | `0` |
| `atanh(x)` | 逆双曲線正接(-1<x<1) | `atanh(0.5)` | `0.5493061443` |

### べき乗・平方根・対数

| 関数 | 説明 | 例 | 結果 |
|---|---|---|---|
| `sqrt(x)` | 平方根 | `sqrt(16)` | `4` |
| `cbrt(x)` | 立方根 | `cbrt(27)` | `3` |
| `pow(x,y)` | xのy乗 | `pow(2,10)` | `1024` |
| `exp(x)` | eのx乗 | `exp(1)` | `2.718281828` |
| `log(x)` | 常用対数(底10) | `log(1000)` | `3` |
| `ln(x)` | 自然対数(底e) | `ln(2.718281828)` | `1` |
| `log2(x)` | 2進対数 | `log2(1024)` | `10` |

### 比較・範囲・整数論

| 関数 | 説明 | 例 | 結果 |
|---|---|---|---|
| `min(a,b)` | 小さい方 | `min(3,7)` | `3` |
| `max(a,b)` | 大きい方 | `max(3,7)` | `7` |
| `clamp(x,min,max)` | xをmin〜maxの範囲に収める | `clamp(15,0,10)` | `10` |
| `gcd(a,b)` | 最大公約数 | `gcd(12,18)` | `6` |
| `lcm(a,b)` | 最小公倍数 | `lcm(4,6)` | `12` |
| `mod(a,b)` | 数学的な剰余(符号は除数(b)に従う。`%`とは異なる) | `mod(-7,3)` | `2` |

### 組合せ論・乱数

| 関数 | 説明 | 例 | 結果 |
|---|---|---|---|
| `ncr(n,r)` | 組合せ(nCr) | `ncr(5,2)` | `10` |
| `npr(n,r)` | 順列(nPr) | `npr(5,2)` | `20` |
| `rand()` | 0以上1未満の乱数 | `rand()` | 例: `0.4213` |
| `rand(lo,hi)` | lo以上hi未満の乱数 | `rand(1,10)` | 例: `7.62` |
| `randint(lo,hi)` | **整数**の乱数(両端を含む) | `randint(1,6)` | サイコロ、`1`〜`6` |

### 丸め・その他

| 関数 | 説明 | 例 | 結果 |
|---|---|---|---|
| `abs(x)` | 絶対値 | `abs(-5)` | `5` |
| `floor(x)` | 切り捨て | `floor(3.7)` | `3` |
| `ceil(x)` | 切り上げ | `ceil(3.2)` | `4` |
| `round(x)` | 四捨五入 | `round(3.5)` | `4` |
| `int(x)` | 0方向への切り捨て(小数部を捨てる) | `int(3.9)` | `3` |

### 直前の結果を使う

| 関数 | 説明 | 例 |
|---|---|---|
| `ans` | 直前の計算結果 | `ans*2` |
| `ans(n)` | n個前の結果(1=直前) | `ans(1)+ans(2)+ans(3)`(直近3件の合計) |

### 定数・演算子

| 記号 | 説明 | 例 | 結果 |
|---|---|---|---|
| `pi` | 円周率 π | `2*pi` | `6.283185307` |
| `e` | 自然対数の底 | `e^2` | `7.389056099` |
| `+ - * /` | 四則演算 | `(3+4)*2/7` | `2` |
| `^` | べき乗 | `2^8` | `256` |
| `%` | 剰余(符号は被除数(左側)に従う) | `-7%3` | `-1` |
| `!` | 階乗 | `5!` | `120` |
| `( )` | 優先順位のグループ化 | `(1+2)*(3+4)` | `21` |

## 例: 整数の乱数を正しく得る方法

`rand(min,max)` は上限を**含まない**ため、`int(rand(1,10))` では `1`〜`9` しか出ず、`10` は絶対に出ません。整数の乱数を**両端を含めて**得たい場合は `randint(min,max)` を使ってください。

```
randint(1,10)   ->  1〜10(両端を含む)
randint(1,6)    ->  サイコロ(6面)
```
